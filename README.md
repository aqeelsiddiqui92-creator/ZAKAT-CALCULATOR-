# Zakat Calculator Android Project

This is a WebView Android wrapper for the supplied single-file Zakat Calculator.
Upload the project to a GitHub repository, then use Actions → Build Zakat Calculator APK → Run workflow.
The generated debug APK is uploaded as the `Zakat-Calculator-APK` artifact.
